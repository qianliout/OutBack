---
name: markdown-formatter
description: 文档格式化、MD 笔记编写、格式约束，或使用 format md/markdown style 等指令时自动启用。
---

# Markdown 格式化专家 (Markdown Formatter)

**铁律 (IRON LAW)**：
- 绝对不要使用过于复杂的嵌套格式！
- 不使用嵌套的 `-` 或 `*` 层级（最多一层）！
- 不要使用**加粗** 或**斜体**。

## 工作流 (Workflow)

## 反模式 (Anti-Patterns - 绝对不要做)
- ❌ 不要写出这样的列表：
  - 第一层
    - 第二层
      - 第三层
- ❌ 不要忽略标点符号前后的空格规范。